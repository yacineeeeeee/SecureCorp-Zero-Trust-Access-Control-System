# SecureCorp-Zero-Trust-Access-Control-System
SecureCorp is a Zero Trust distributed system built with Python microservices. It implements Kerberos-inspired authentication, RBAC + ABAC authorization, and a policy engine (PDP). Includes vulnerable and secure versions with attack simulations (replay, tampering, escalation).
